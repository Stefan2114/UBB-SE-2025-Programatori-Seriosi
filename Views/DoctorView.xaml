<Page
    x:Class="Team3.Views.DoctorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Team3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button x:Name="BackButton"
                Content="Back"
                Click="BackButton_Click"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,10"/>

        <!-- Title -->
        <TextBlock Text="Doctor Statistics"
                   Grid.Row="1"
                   Style="{ThemeResource TitleLargeTextBlockStyle}"
                   Margin="0,0,0,20"/>

        <!-- Time Period Selection -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    Spacing="10" 
                    Margin="0,0,0,20">
            <TextBlock Text="Select Time Period:"
                       VerticalAlignment="Center"/>
            <ComboBox x:Name="TimePeriodComboBox"
                      Width="200"
                      SelectionChanged="TimePeriodComboBox_SelectionChanged">
                <ComboBoxItem Content="Last Week"/>
                <ComboBoxItem Content="Last Month"/>
                <ComboBoxItem Content="Last 6 Months"/>
                <ComboBoxItem Content="Last Year"/>
            </ComboBox>
        </StackPanel>

        <!-- Headers and List Container -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Headers -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Doctor Name" 
                           Grid.Column="0"
                           Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <TextBlock Text="Hours Worked" 
                           Grid.Column="1"
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           TextAlignment="Center"/>
                <TextBlock Text="Patients Treated" 
                           Grid.Column="2"
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           TextAlignment="Center"/>
            </Grid>

            <!-- Doctor List -->
            <ListView x:Name="DoctorListView"
                      Grid.Row="1"
                      Margin="0,10,0,0"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" 
                                       Grid.Column="0"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="180"/>
                            <TextBlock Text="{Binding TotalHoursWorked}" 
                                       Grid.Column="1"
                                       TextAlignment="Center"/>
                            <TextBlock Text="{Binding PatientsTreated}" 
                                       Grid.Column="2"
                                       TextAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
