<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Team3.Views.DepartmentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Team3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button x:Name="BackButton" 
                Content="Back" 
                Click="BackButton_Click"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,20"/>

        <!-- Department List -->
        <ScrollViewer Grid.Row="1">
            <ListView x:Name="DepartmentListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="10">
                            <TextBlock Text="{Binding Name}" 
                                       FontWeight="Bold" 
                                       TextTrimming="CharacterEllipsis" 
                                       MaxWidth="200"/>
                            <TextBlock Text="Doctors: {Binding DoctorCount}" />
                            <TextBlock Text="Equipment: {Binding EquipmentCount}" />
                            <TextBlock Text="Rooms: {Binding RoomCount}" />
                            <TextBlock Text="Internees: {Binding InterneeCount}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
